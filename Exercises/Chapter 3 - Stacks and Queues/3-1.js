class TripleStacks {
  constructor(stacksQty) {
    this.stacks = [];
    this.stacksQty = stacksQty;
  }

  push(val, stackNum) {
    const key = 1;
  }

  pop(stackNum) {}

  peek(stackNum) {}
}

let stack = new TripleStacks(3);

stack.push(10, 3);
